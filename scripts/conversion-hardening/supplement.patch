diff -ruN '--exclude=node_modules' pv-primary/anam-token-worker/src/stripe.js pv-conversion/src/anam-token-worker/src/stripe.js
--- pv-primary/anam-token-worker/src/stripe.js	2026-09-10 01:27:15.788568780 +0000
+++ pv-conversion/src/anam-token-worker/src/stripe.js	2026-09-10 01:41:39.881624170 +0000
@@ -150,7 +150,12 @@
 export async function createSignalCreditCheckout(env, identity, packId, origin, client = null, requestId = "") {
   const pack = configuredPack(env, packId);
   const stripe = client || stripeClient(env);
-  const purchaseId = normalizedRequestId(requestId) || crypto.randomUUID();
+  const suppliedRequestId = typeof requestId === "string" ? requestId.trim() : "";
+  const normalizedId = normalizedRequestId(suppliedRequestId);
+  if (suppliedRequestId && !normalizedId) {
+    throw new StripePurchaseError("invalid_checkout_request", "Invalid checkout request", 400);
+  }
+  const purchaseId = normalizedId || crypto.randomUUID();
   const metaEmailHash = await hashNinaMetaEmail(identity?.user?.email || "");
   let purchase = await purchaseById(env, purchaseId);
   const now = new Date().toISOString();
@@ -162,6 +167,13 @@
     }
     if (purchase.stripe_checkout_session_id && purchase.status === "open") {
       const existingSession = await stripe.checkout.sessions.retrieve(purchase.stripe_checkout_session_id);
+      if (existingSession?.payment_status === "paid") {
+        return { requestId: purchaseId, sessionId: existingSession.id, status: "paid", url: "" };
+      }
+      if (existingSession?.status === "expired") {
+        await markSessionStatus(env, existingSession, "expired");
+        throw new StripePurchaseError("checkout_expired", "Checkout Session expired. Start a new checkout.", 409);
+      }
       if (existingSession?.url) {
         return { requestId: purchaseId, sessionId: existingSession.id, status: purchase.status, url: existingSession.url };
       }
diff -ruN '--exclude=node_modules' pv-primary/anam-token-worker/test/stripe.test.js pv-conversion/src/anam-token-worker/test/stripe.test.js
--- pv-primary/anam-token-worker/test/stripe.test.js	2026-09-10 01:29:37.986014123 +0000
+++ pv-conversion/src/anam-token-worker/test/stripe.test.js	2026-09-10 01:42:44.344463829 +0000
@@ -220,6 +220,14 @@
   );
 });
 
+test("malformed checkout request IDs are rejected instead of losing retry idempotency", async () => {
+  await assert.rejects(
+    () => createSignalCreditCheckout(configuredEnv(paymentDb()), { user: { id: "u" }, clerkUserId: "c" }, "signal_60", "https://parallelvisionlabel.com", mockStripe(), "not-a-uuid"),
+    error => error instanceof StripePurchaseError && error.code === "invalid_checkout_request"
+  );
+});
+
 test("60-credit starter pack creates a validated €3.50 Checkout purchase", async () => {
   const db = paymentDb();
   const env = configuredEnv(db);
diff -ruN '--exclude=node_modules' pv-primary/js/nina-access.js pv-conversion/src/js/nina-access.js
--- pv-primary/js/nina-access.js	2026-09-10 01:28:19.826894035 +0000
+++ pv-conversion/src/js/nina-access.js	2026-09-10 01:41:39.881315100 +0000
@@ -97,7 +97,7 @@
 const ninaSignalCredits = byId("ninaSignalCredits");
 const ninaLiveTime = byId("ninaLiveTime");
 const ninaAccountSignOut = byId("ninaAccountSignOut");
-const SIGNAL_CREDIT_PACK_IDS = new Set(["signal_60", "signal_150", "signal_300", "signal_600"]);
+const SIGNAL_CREDIT_PACK_IDS = new Set(["signal_60", "signal_150", "signal_300", "signal_600", "signal_1200"]);
 const NINA_CREDIT_CHECKOUT_STATE_KEY = "nina_signal_credit_checkout_v1";
 const NINA_CREDIT_SNAPSHOT_KEY = "nina_signal_credit_snapshot_v1";
 const NINA_CREDIT_SNAPSHOT_TTL_MS = 10 * 60 * 1000;
@@ -731,6 +731,7 @@
         <button class="nina-credits-pack" type="button" data-pack-id="signal_60"><span class="nina-credits-pack-copy"><span class="nina-credits-pack-time">6 MIN</span><span class="nina-credits-pack-title">60 Signal Credits</span><span class="nina-credits-pack-description">A short live signal with Nina.</span></span><strong>€3.50</strong></button>
         <button class="nina-credits-pack" type="button" data-pack-id="signal_150"><span class="nina-credits-pack-copy"><span class="nina-credits-pack-time">15 MIN</span><span class="nina-credits-pack-title">150 Signal Credits</span><span class="nina-credits-pack-description">More time to stay in the conversation.</span></span><strong>€8.99</strong></button>
         <button class="nina-credits-pack" type="button" data-pack-id="signal_600"><span class="nina-credits-pack-copy"><span class="nina-credits-pack-time">60 MIN</span><span class="nina-credits-pack-title">600 Signal Credits</span><span class="nina-credits-pack-description">For returning conversations.</span></span><strong>€27.99</strong></button>
+        <button class="nina-credits-pack" type="button" data-pack-id="signal_1200"><span class="nina-credits-pack-copy"><span class="nina-credits-pack-time">120 MIN</span><span class="nina-credits-pack-title">1200 Signal Credits</span><span class="nina-credits-pack-description">For long return visits.</span></span><strong>€54.99</strong></button>
       </div>
       <details class="nina-credits-about">
         <summary><span class="nina-credits-about-closed">About Signal Credits +</span><span class="nina-credits-about-open">About Signal Credits −</span></summary>
diff -ruN '--exclude=node_modules' pv-primary/scripts/test-nina-web-browser.py pv-conversion/src/scripts/test-nina-web-browser.py
--- pv-primary/scripts/test-nina-web-browser.py	2026-09-10 01:30:36.196107544 +0000
+++ pv-conversion/src/scripts/test-nina-web-browser.py	2026-09-10 01:43:05.785771407 +0000
@@ -55,6 +55,7 @@
   page=browser.new_page(viewport={'width':width,'height':height});errors=[];page.on('pageerror',lambda e:errors.append(str(e)))
   page.set_content(html);page.add_script_tag(content=pre+source+trialmodule+webmodule+end)
   page.evaluate('testOpen()');page.wait_for_timeout(200)
+  assert page.locator('[data-pack-id="signal_1200"]').count()==1
   assert page.evaluate("testState().web") is True
   assert page.evaluate("requests.filter(x=>x.url.includes('/live/activate')).length")==0
   page.evaluate('testConnect()');page.wait_for_selector('.nina-web-audio-check:not([hidden])')
